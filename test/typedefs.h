#include <stdbool.h>

// Nested typedefs
typedef int inte;
typedef inte integ;
typedef integ intege;
typedef intege integer;

integer add4(intege, integ, inte, int);

// Struct
typedef struct {
  int a;
  long b;
  float c;
} some_nums;

bool crunch(some_nums);
