#include <stdbool.h>

// Nested typedefs
typedef int inte;
typedef inte integ;
typedef integ intege;
typedef intege integer;

integer add4(intege, integ, inte, int);

// Struct
struct some_nums {
  int a;
  long b;
  float c;
};

bool crunch(some_nums);
